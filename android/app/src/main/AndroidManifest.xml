<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            AdMob 應用程式 ID。這一項是必填的：少了它 Google Mobile Ads SDK
            會在啟動時直接丟例外讓 App 當掉，不是「廣告不出現」而已。

            目前填的是 Google 官方的測試 App ID，任何人都能用。上架前換成
            AdMob 後台那一組（格式 ca-app-pub-XXXX~YYYY，波浪號不是斜線；
            斜線那種是廣告單元 ID，填錯一樣會當）。
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Android 13 以上，讀取廣告 ID 需要明確宣告。SDK 自己也會合併進來，
        寫在這裡是為了讓 Play Console 的「資料安全」表單有據可查——
        送審時要一併聲明本 App 會收集廣告 ID。
    -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
</manifest>
