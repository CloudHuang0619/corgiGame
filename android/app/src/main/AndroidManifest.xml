<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            AdMob 應用程式 ID（Android 版的 Corgidoku）。

            這一項是必填的：少了它 Google Mobile Ads SDK 會在啟動時直接丟
            例外讓 App 當掉，不是「廣告不出現」而已。

            注意分隔符號是波浪號 ~，那是應用程式 ID；斜線 / 那種是廣告單元
            ID，填錯一樣會當。iOS 是另一組 ID，填在 Info.plist 的
            GADApplicationIdentifier，兩者不共用。

            這串不是機密——它會編進每一個 APK 裡，任何人解包都看得到。
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5561064840285913~1435744822" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Android 13 以上，讀取廣告 ID 需要明確宣告。SDK 自己也會合併進來，
        寫在這裡是為了讓 Play Console 的「資料安全」表單有據可查——
        送審時要一併聲明本 App 會收集廣告 ID。
    -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
</manifest>
